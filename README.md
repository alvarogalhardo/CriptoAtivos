# Archived: original console draft

This branch preserves the earliest version of CriptoAtivos: a single-package
Java console application with no build tool and no persistence, kept only for
reference.

It was never committed anywhere at the time, so it is archived here rather than
lost. The academic Maven + Oracle JDBC version that followed it is tagged
`v0-academic-baseline`. Current development is on `main`.
